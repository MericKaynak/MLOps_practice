[project]
name = "mlops-practice"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
  "pandas",
  "numpy",
  "scikit-learn",
  "mlflow",
  "optuna",
  "optuna[mlflow]",
  "dvc"
]
